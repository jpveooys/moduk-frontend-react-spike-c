# React MOD.UK Frontend spike

This is some code demonstrating one approach of providing React versions of MOD.UK Frontend components.

To start the project, run:

```shell
pnpm install
```

Followed by:

```shell
pnpm dev
```
